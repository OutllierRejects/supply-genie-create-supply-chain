import { ServerlessInstance, PluginOptions } from "./types";
declare class UvRequirementsPlugin {
    private serverless;
    private options;
    hooks: Record<string, () => void>;
    private config;
    constructor(serverless: ServerlessInstance, options: PluginOptions);
    /**
     * Validate that the service uses a Python runtime
     */
    private validatePythonRuntime;
    /**
     * Main method to generate requirements
     */
    generateRequirements(): Promise<void>;
    /**
     * Show generation summary
     */
    private showSummary;
    /**
     * Enhanced logging with different levels
     */
    private log;
}
export default UvRequirementsPlugin;
//# sourceMappingURL=index.d.ts.map